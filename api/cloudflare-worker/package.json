{
  "name": "datadiggers-chat-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that proxies chat requests from the DataDiggers website to Anthropic's Claude API.",
  "private": true,
  "main": "worker.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
